# Docker Compose file for Cytoscape App Store


db:
  image: mysql

appstore:
  build: .
  expose:
    - "8000"
  links:
    - db
  volumes:
    - /usr/src/app/static
  env_file: .env
  command: /usr/local/bin/gunicorn docker_django.wsgi:application -w 2 -b :8000
  volumes:
    - .:/code
